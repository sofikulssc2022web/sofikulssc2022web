#include<stdio.h>
#include<string.h>
int main(){
char s[100];
fgets(s,100,stdin);
 strlwr(s);
//puts(s);

int max=0;
char v;
int len=strlen(s);
for(int i=0;i<len-1;i++)
{
     int c=0;
  for(int j=0;j<len-1;j++)  {

    if(s[i]==s[j]){
        c++;

    }
  }
  if(c>max){
    max=c;
    v=s[i];
  }

}
printf("%c",v);




return 0;
}

